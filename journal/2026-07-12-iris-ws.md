---
type: journal
title: "PRV10IRIS5: rede final 192.168.0.x + stack VK/EIP corrigido (env vars)"
description: "WS renumerada p/ 192.168.0.189, PLC 192.168.0.20; auditoria achou stack em crash-loop com IPs antigos; env vars/composes corrigidos em 4 diretórios"
tags: [iris-ws, iris-scds, vk, gm, rede, docker, eip, prv10iris5]
timestamp: 2026-07-12
project: "03007"
product: visionking
language: pt-BR
status: done
---

# 2026-07-12 — PRV10IRIS5: rede 192.168.0.x definitiva + serviços corrigidos

## Feito
- Acesso validado pós-visita: cadeia energizar→GRUB→autologin kiosk OK; **BIOS ainda F5** (flash F17 não realizado; pendrive Twist pronto).
- **Renumeração final**: WS `eth_painel` → **192.168.0.189/24** (netplan, sem gateway — `.0.1` mudo); PLC re-endereçado pelo Pedro → **192.168.0.20** (ping 0,3 ms); dev PC → alias **192.168.0.204** (+ 10.10.0.204 e 192.168.15.204 mantidos). Faixa 10.10.0.x abandonada.
- **Auditoria de serviços** (pedido do Pedro): stack VK/EIP inteiro em crash-loop com IPs antigos. Fontes de config REAIS descobertas via labels compose (`working_dir|config_files`): `~/deploy-stellantis` (+`infra/`), `~/deploy` (!), `~/eip-stack` (!), `~/deploy-eip-cmd` (só templates). 
- **Correções aplicadas** (backups .tgz + .bak no home):
  - `192.168.15.189 → 192.168.0.189` em .env + ymls dos 4 diretórios (auto-referências Redis/Rabbit/DB/host);
  - `PLC_IP 192.168.15.121 → 192.168.0.20` (plc-monitor-eip);
  - `~/eip-stack/.env`: `PLC_KEY 192.168.15.123 → 192.168.0.20` e **`EIP_IFACE enp5s0 → eth_painel`** (quebrado desde a renomeação de interfaces!);
  - `~/eip-stack/tag-client/tags.json`: `plc_ip → 192.168.0.20`.
  - 31 containers recriados via compose de cada projeto.
- **Resultado**: inference+pixel-to-object consumindo filas; plc-monitor conecta no PLC (restam "Path segment error" = tag `Teste:O.Data[x]` inexistente no programa do PLC novo — item de PLC, não de rede); eip-adapter e tag-client estáveis, reads passando.

## Adendo — EDS iris03007 + tags.json (integração PLC fechada)
- EDS registrado no Studio 5000 e módulo adicionado como **`PRV10IRIS5`** (nome do device na rede/diagrama elétrico GM) → Class 1 "Running"; confirmado por `ss`: PLC conectado no 44818 do adapter.
- "Path segment error" pós-EDS era **nome de módulo**: tags.json antigo usava prefixo `Teste:` (POC Stellantis).
- **tags.json novo gerado do EDS** `STROKMATIC-COMM-V1_iris03007.eds` (fonte: `sdk/strokmatic-eip/adapter/eds/`; layout Anexo D IRIS-03007 v0.2): Assem150 O→T 128 B — Sys_* (bytes 0-4), 4 eixos E1/E2/E4R/E4L (8-55), Tracks 0/1 (56-71). **83 tags, 0 falhas.**
- Gotchas: (a) driver do plc-monitor NÃO aceita sintaxe pycomm3 `{count}` → campos multibyte (Pos/LongPos/Sys_Mode/LastCmdSeq DINT/INT) expandidos em `_B0.._B3` byte a byte, decodificação little-endian fica no consumidor (D3 do EDS); (b) keys antigos `Tag_Material_*`/`Tag_Corte_*` extintos — conferir consumidores Redis; (c) `Track_x_Phase` (3 bits) fora do arquivo — mascarar byte se necessário.
- Backups: `tags.json.bak-teste` no deploy-stellantis.

## Adendo 2 — mirror-test PLC↔Redis (PASS 8/8)
- **Ferramenta nova**: `~/eip-stack/tools/mirror-test.sh` — testa os DOIS sentidos do contrato Redis (docs/redis-contract.md): (A) escrita explícita pylogix no PLC → hash frio `tags:<plc>:proc` (updated_at fresco); (B) `XADD cmd:<plc>` class=config → escrita no PLC + `audit:<plc>` outcome=applied (latência ~53 ms) + ciclo Redis→PLC→poll→Redis. Tag de teste: `WS_PVI` (STRING, propriedade da WS; restaurada ao final). **Nunca usa class=motion** (age-discard + segurança).
- **Causa raiz do 1º FAIL**: `eip-stack/tag-client/tags.json` ainda tinha tags do PLC da BANCADA (`Heartbeat/Pos/ErrCount`) — inexistentes no controlador GM. `GetTagList` via pylogix revelou o programa real: `IRIS_Ready/InCycle/CycleComplete/Fault`, `WS_ScanActive`, `GM_PVI/GM_Style/WS_PVI` (STRING), `Sys_Mode`, `GM_CarEntering`. tags.json reescrito (grupos status 200ms + proc 500ms; echo_map vazio — `JogCmd/JogEcho` não existem aqui); backup `.bak-bench`.
- Hash frio só nasce quando o LOTE inteiro do grupo lê com sucesso (regra do contrato) — silêncio no log do tag-client NÃO significa leituras OK; conferir `HGETALL tags:<plc>:<grupo>`.
- Hot path do adapter confirmado vivo: `io:in:192.168.0.20` no Redis; conexão do módulo no PLC visível no GetTagList (`Cxn:_200424052C962C64` = path do EDS).
- Gotcha awk: `exit` executa `END` — parser de audit imprimia outcome 2×.
- **README de comissionamento** criado em `~/eip-stack/tools/README.md` (o que o mirror-test valida passo a passo, inspeção manual do Redis, tabela de quem-consome-qual-config, regra de ouro do silêncio do tag-client).
- **Proveniência do eip-stack**: NÃO é novo — é o stack do repo `strokmatic/strokmatic-eip` (issue #4, merged develop 2026-07-10), deployado na WS em 2026-07-10 p/ bench (RESULTS-2026-07-10.md). Nesta sessão: correção de config (.env, tags.json p/ tags reais GM) + ferramenta mirror-test.sh + README (novos). Candidatos a upstream no repo: mirror-test.sh + README → avaliar PR.

## Adendo 3 (2026-07-13) — iris-bench-panel: wiring consertado
- Painel (deploy Pedro, dev em outra sessão) estava 90/114 campos null + 100% dos comandos rejeitados. **3 causas**: (1) hot-bridge (Fase B) NÃO existe no repo strokmatic-eip — `tags:<key>:hot` órfão; (2) cmd stream sem `seq/ts_ms/group` → tag-client auditava `bad command entry: 'seq'`; (3) telemetria cold lia hash plano `tags:<key>` vs contrato `tags:<key>:<grupo>`.
- **Diretiva do Pedro**: PLC atual + contratos dos containers da WS mandam; painel se adapta.
- **GetTagList completo (332 tags!)**: programa do PLC é RICO (WSCmd_Jog/Home/JogRefresh, ServoAxisN.Cfg, alarmes, zonas, GM_*) — o "programa mínimo" de ontem era artefato de `head -40`. Lição: nunca truncar inventário de tags.
- **Correções no painel** (fonte extraído da imagem p/ `~/iris-bench-panel/backend/` + bind-mount no compose): `transports.py` ColdRedis conforme redis-contract.md (grupos + seq/ts_ms/group); `app.py` `HOT = COLD` (comandos motion pelo stream contratual até o hot-bridge nascer); `tagmap.py` re-roteado p/ tags reais (cold qualificado por grupo, resto direct; TODOs p/ automação: WSCmd_StopAll/Bypass/Mode/JogSpeedN não existem).
- **tags.json do eip-stack**: +grupo `panel` (500 ms, 11 tags de alarme/barreira/GM) e IRIS_PVI_ACK no proc; backup `.bak-pre-panel`.
- **Resultado**: null 90→22/114 (restam hot/axis, aguardando Fase B); staleness cold 0,47 s; comando `step_size=2.5` → audit `applied` em **7 ms** → readback no PLC = 2.5 ✓. WSCfg_JogStepSize ficou em 2.5 (valor de teste).
- Pendências: hot-bridge (issue Fase B no strokmatic-eip); fonte do painel corrigido só na WS — autor deve absorver os patches no repo de origem; upstream mirror-test/README no strokmatic-eip.

## Decisões
- cam-acquisition-1/2 em restart = esperado (sem câmeras plugadas, eth_cam* sem link) — não mexer.
- Refs a 192.168.15.190/.2 (training/infra do escritório) mantidas nos backend-ds ymls — inalcançáveis do painel; topologia futura decide.
- DHCP p/ laptop de serviço: informativo entregue (NM `ipv4.method shared` recomendado; nunca na eth_painel). Não implementado.

## Pendências
- Flash BIOS F17 + AC BACK=Always On (pendrive pronto; visita física).
- Tags do PLC: alinhar programa (Teste:O.Data) com plc-monitor — c/ equipe de automação.
- Câmeras nas eth_cam* (IPs .1 por subrede) e cabo eth_gmplant.
- `tags.json`/`.env` do eip-stack: consolidar fonte única de PLC IP (hoje 3 lugares).

## Links
- journal/2026-07-11-iris-ws.md (bloco anterior)
- config/network/local-machines.md — entrada PRV10IRIS5 atualizada
