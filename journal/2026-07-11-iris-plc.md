---
type: Session Log
title: 2026-07-11 — iris-plc — jog/homing v5 escritos em L5X + jobdata GM real integrado (contrato v0.99.8)
description: Dia denso do 03007 — rede física resolvida no WD GM (dual-NIC, célula 192.168.1.x), layout real do scan corrige o C3, geramos o Servo v5 (jog Halt + homing touch probe + mm) e o pacote de integração do Jobdata com o zc_JobData real (Station[11]); contrato avançou v0.99.4→v0.99.8; tudo commitado (pmo 59056f7).
tags: [iris, plc, rockwell, l5x, jobdata, gccs, homing, jog, gm]
timestamp: 2026-07-11
session: iris-plc
project: "03007"
language: pt-BR
---

# 2026-07-11 — iris-plc (jog/homing v5 + jobdata GM)

## Feito

- **Rede física (WD GM + EPLAN)**: PC IRIS = `-E16PC1` (EPLAN) / `PRV10IRIS5`
  (`.165`, WD GM); PLC `PRV10A1` com **2 portas alocadas pela GM** (EN0=`.160`
  planta · EN1=`192.168.1.12` célula, vizinho do PLC GM `.11`) — decisão:
  **rede interna = célula GM**; WS dual-NIC; pedir porta+IP p/ WS NIC2 e
  Anybus (V1); câmeras fora do switch da célula. Contrato §0.2 v0.99.4/5.
- **Layout real do scan** (COPs do programa Willer): In **21 B**/drive
  (2 error words) offsets 0/21/42/63 · Out **8 B** (TouchProbeFunc INT)
  0/8/16/24 — corrigiu o C3 §3.1 (v0.99.7) que assumia 19/10 B.
- **Servo v5 escrito em L5X** (gerador Python → XML/CDATA validado):
  Seção 0 (mm por `HomeOffsetRaw`/`CountsPerMM`), Seção 3 jog (alvo-longínquo
  + **Halt bit 8**), Seção 4 homing (**touch probe** — switch na entrada de
  probe do drive; sem 0x6060), StopRequest; + R030_ManualMotion (deadman,
  1-eixo, carro presente), R031_Homing (4R/4L→E1→E2), R003 corrigido
  (`Fault_Reset_Req1`), 34 tags CSV. Rebaseado no r2 (código idêntico ao r1).
- **Jobdata GM real**: exports do `zc_JobData` v1.303 **idênticos nos 3 ACDs**
  ODD; payload útil = `Valid` + **`zz_Data`** (~36 B: Options INT[8], Style,
  **VID DINT** — resolve o "domínio do PVI"). Pacote de integração gerado:
  `UDT_StationJob`, `GM_Com_In` v2 (**`Station[11]`**: 0=inspeção,
  1-10=revisões; GM efetiva=0-2; ~424 B ✓ <500 B; +`LineRunning`, −PVI/Style/
  LongPosition), `WS_Com` v2 (−Retrabalho_*), R020 patchado (gate `Valid` +
  COP job → `WS_Queue_Job[2]`; corrige 2 copy-paste bugs), R006_MapGM
  (watchdog heartbeat 2 s; futuro CPS+RunOK+FLL), R052_Publish, R090_GMSim.
- Contrato **v0.99.4 → v0.99.8** + renders; commit pmo **59056f7** (38 files).

## Complemento retroativo — bloco de 10/07 (não havia entrada)

- **Snapshot PLC 07-10** (`plc/2026.07.10_programa-plc-rockwell/`): 33 rungs,
  9 rotinas STEP/MIRROR, primeiros UDTs GM/WS — diff completo no README;
  10 divergências vs contrato mapeadas (anticolisão invertida→corrigida no r2,
  LongPosition×LineRunning, PVI_ACK sem unlatch etc.).
- **Contrato v0.99.3**: PVI/Style = DINT (decisão Pedro — o programa estava
  certo); retrabalho por ESTAÇÃO (não região): `GM_Station[5]` → depois [11].
- **Pacote de feedback ao Willer** (`handoffs/2026.07.10_feedback-programa-plc-willer/`,
  commit pmo 453f230): tom WIP, 9 pontos + typos (Incialize→Inicialize,
  Inerters→Inverters) + PDFs do contrato/arquitetura anexos.
- **Issue visionking-plc-monitor-eip#61** criada (caminho de escrita de baixa
  latência, Fase A) e reescrita **autossuficiente** (William sem acesso ao pmo):
  schema do Stream `plc:cmd` + exemplos XADD, write-map/whitelist, deadman é
  do PLC, relógio único, ambientes de teste; solução 100% Classe 3 standalone —
  OpENer explicitamente otimização futura opcional (caminho seguro do Pedro).

## Decisões (Pedro)

- Rede interna do sistema = rede privada da célula GM (`192.168.1.x`).
- Homing = offset por software (drive interno → `HomeOffsetRaw`; ActualPosition
  vira 0 no Home; tudo em mm dali) — validado; touch probe p/ captura.
- Boot nunca movimenta eixos; `Station[11]` (1 inspeção + 2 revisões efetivas,
  preparado p/ 10); PVI/Style = `VID`/`Style` do Jobdata; térmico ×6 é WIP
  consciente do Willer (não cobrar replicação ainda).

## Pendências

- Willer: importar v5 + jobdata (READMEs com ordem e 3 edições manuais:
  JSRs, `R011 R4 XIC→XIO`, config `GMHb_TimeoutScans`); validar bancada
  (checklist 8 passos); fiação do switch de home no probe do drive.
- Programa: `ConveyorOK`/`InCycle`/GCCS no `GM_Com_Out`; R020→`Cmd*`
  (movimentação dinâmica Seção 2-b CSI); integrador D4 → `GM_LongPosition`.
- GM (V1): mapeamento ODD v1×v2 (semântica dos Options); porta+IP WS/Anybus.
- P2 (bancada 24 V): 0x6060/0x60FF/0x6081/0x6077 — libera níveis de jog.

## Links

- Commit: pmo `59056f7` · specs `IRIS-03007-Contratos-Integracao.md` v0.99.8
- Pacotes: `plc/2026.07.11_update-jog-homing/` · `plc/2026.07.11_update-jobdata/`
- Referência: `reference/zc_JobData-odd-exports/` (README = análise)
- Journals irmãos: [[2026-07-09-iris-contratos]] · [[2026-07-09-eip-stack]]
