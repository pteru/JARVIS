---
type: Session Log
title: 2026-07-10 — eip — issues #7 (PEL reclaim) e #8 (rollup) mergeadas via PR #9
description: PR #9 mergeado no develop fecha as issues #7 e #8 do strokmatic-eip — canal de comando do tag-client agora sobrevive a crash sem órfãos (PEL drain no startup) e o rollup de precisão pós-#5 entrou (config, try-scope do audit, higiene do poller, healthchecks, semântica do contrato); auto-review APPROVE WITH COMMENTS com todos os pontos endereçados.
tags: [eip, ethernet-ip, strokmatic-eip, tag-client, redis, pel]
timestamp: 2026-07-10
session: eip-issues-7-8
project: "03007"
language: pt-BR
---

# 2026-07-10 — eip (issues #7 e #8 → PR #9 merged)

## Feito

- **PR #9 MERGED no develop** (19:04Z); issues **#7** e **#8** fechadas
  manualmente (keywords `closes` só atuam no branch default, que ainda é
  `master` — trunk real é `develop`).
- **#7 — PEL reclaim**: `CommandThread._drain_pending` lê a própria PEL
  (`xreadgroup "0"`) pelo caminho normal do `execute_batch` antes do loop
  `">"`. Stop entregue-e-não-ackado numa janela de crash não é mais perdido.
  Guard de progresso (PEL idêntica 2× → RuntimeError, crash-only) + log INFO
  de recovery. e2e simula a janela de crash (parquear entradas sem ack,
  restart, XPENDING 0).
- **#8 — rollup**: ConfigError em ints inválidos e tags-string; try do
  `execute_batch` restrito a I/O de PLC (audit/ack fora — blip de Redis não
  mislabela `applied` como `error`; echo com serialização falha só some);
  higiene do hash do poller (hdel de tags removidas; single-writer documentado);
  healthcheck do redis via `REDISCLI_AUTH` (senha fora do argv) + `depends_on`
  gated; contrato: precedência stop×stale, campos `detail`/`entry_id`,
  unknown class, clock same-host, override por `profiles` (verificado
  empiricamente no Compose v2.17.3 — `config --services` erra cosmético,
  `up` funciona).
- Auto-review do cron: **APPROVE WITH COMMENTS** — 6 pontos aplicados no ato,
  2 deferidos (backport de helper; validação de tipo de
  `discard_classes`/`echo_map` — candidato a touch futuro). Validação final:
  units 33/33, e2e 4/4, exit 0.
- Interrupção de sessão no meio da #8: trabalho não-commitado sobreviveu;
  agent retomado via transcript e terminou sem redo (ledger salvou o contexto).

## Decisões

- Um branch/PR para as duas issues (mesmos arquivos) — commits separados por
  concern.
- W4 do review (senha visível via docker inspect no healthcheck): trade-off
  padrão aceito; entra num futuro hardening guide.

## Pendências

- **Branch default do repo ainda é `master`** — recomendar trocar p/ `develop`
  (evita o gotcha do auto-close e aponta PRs novos pro trunk certo). Decisão
  do Pedro.
- Operacional: rotação da senha Redis do histórico; artefatos da cert ODVA
  (Matheus). Fase A do 03007 com o William.
- Deferidos do review: validação de tipos de `discard_classes`/`echo_map`.

## Links

- PR: strokmatic-eip#9 (merged) · Issues: #7 ✓ · #8 ✓
- Journals do dia: [[2026-07-10-eip]] · [[2026-07-10-eip-adapter-move]]
