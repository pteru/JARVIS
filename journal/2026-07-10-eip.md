---
type: Session Log
title: 2026-07-10 — eip — implementação da EIP stack composta (issue #4)
description: Branch feat/eip-stack no strokmatic-eip implementa a issue #4 fim-a-fim — tag-client genérico pylogix (poll + canal de comando com descarte por idade/stop prioritário/audit), compose de 3 serviços com secrets em .env, contrato de chaves Redis documentado e rig de integração cpppo validado; 12 commits locais aguardando push/PR.
tags: [eip, ethernet-ip, pylogix, strokmatic-eip, docker, redis, tag-client]
timestamp: 2026-07-10
session: eip
project: "03007"
language: pt-BR
---

# 2026-07-10 — eip (implementação da EIP stack composta)

## Feito

- **Issue strokmatic-eip#4 implementada** em `feat/eip-stack` (12 commits locais,
  base 911b38d → f8d35b7): spec + plano (8 tasks TDD) executados via
  subagent-driven-development com review por task + review final de branch.
- **tag-client/ genérico** (Python 3.11, pylogix, redis-py): config `tags.json`
  validada; PollThread por grupo (batch read → HASH `tags:<plc_key>:<grupo>` com
  `updated_at` all-or-nothing, regra 5-a); CommandThread (XREADGROUP `tag-client`,
  descarte >300 ms por classe, stop prioritário, 1 audit+1 ack por entry, echo
  opcional); main crash-only (excepthook → exit 1 + watchdog). 26 unit tests.
- **Compose de 3 serviços**: eip-adapter (host net, cap_add em vez de privileged),
  eip-tag-client (bridge, restart always), redis (porta só em 127.0.0.1); senha
  `SparkEyes@@2022` REMOVIDA do YAML → `.env` (histórico ainda a rotacionar).
- **docs/redis-contract.md**: hot/cold/cmd/audit + semântica; README atualizado
  (status real + nota do move pós-Phase 0).
- **Rig de integração validado**: cpppo 5.2.5 (GPL, test-time only) + redis em
  Docker; e2e 3/3 com client real; smoke da stack OK. Causa-raiz do checkpoint
  inicial: YAML folded scalar quebrava os args do cpppo (não era protocolo).
- **2 bugs reais achados pelos reviews**: exceção de PLC I/O matava o batch sem
  audit/ack (corrigido per-entry); `self._stop` shadowava `threading.Thread._stop()`
  → TypeError no teardown (renomeado `_stop_event`).

## Decisões

- Layout: opção B em fases — B-lite agora (OpENer intocado na raiz), `git mv` →
  `adapter/` só depois do Phase 0 (diffs legados alinhados). README corrigido.
- Crash-only é a política p/ exceções de Redis (processo morre, Docker cura);
  PLC I/O nunca derruba o batch.

## Pendências

- ~~Push de `feat/eip-stack` + PR~~ — **PR #5 MERGED no develop no mesmo dia**
  (ver [[2026-07-10-eip-adapter-move]]).
- ~~Follow-ups (issues a abrir)~~ — viraram **#7 e #8, resolvidas via PR #9 no
  mesmo dia** (ver [[2026-07-10-eip-issues-7-8]]).
- Rotação da senha Redis vazada (política do purge 2026-06-17) — segue aberta.
- ~~Phase 0 (archaeology) continua pendente~~ — **impreciso**: inventário +
  re-aplicação Bucket-A já estavam completos no master desde maio (audit
  2026-05-07); correção registrada em [[2026-07-10-eip-adapter-move]]. Anexo D
  quando a Fase B entrar no roadmap — segue aberto.

## Links

- Spec: `docs/superpowers/specs/2026-07-09-eip-stack-design.md` · Plano: `docs/superpowers/plans/2026-07-09-eip-stack.md`
- Issue: https://github.com/strokmatic/strokmatic-eip/issues/4
- Ledger: `workspaces/strokmatic/sdk/strokmatic-eip/.superpowers/sdd/progress.md`
