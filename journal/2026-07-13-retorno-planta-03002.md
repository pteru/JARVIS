---
type: journal
title: "Retorno dos PCs à planta 03002 — conectividade, rename de NICs e câmeras/controladores online"
description: "vk01/vk02 reinstalados na ArcelorMittal: reboot recovery 100%, causa raiz das câmeras/controladores fora (netplan com nomes de NIC pré-troca de MB), fix aplicado pelo Pedro e validação ponta a ponta"
tags: [retorno-planta-03002, vk-producao, arcelor, "03002"]
timestamp: 2026-07-13
project: "03002"
product: VisionKing
language: pt-BR
status: done
---

# 2026-07-13 — Retorno dos PCs à planta 03002

## Feito
- **Acesso restabelecido**: IPs da planta 10.244.70.26 (vk01) / .50 (vk02), SSH **porta 8050** (não 22), via VPN SNX. Gotcha no desktop: rota ZeroTier da bancada (10.179.112.0/24) sombreava a VPN — usar IPs diretos da planta.
- **Reboot recovery 100%**: 29 containers up em cada host, stack novo completo (visionking-*, filas renomeadas, autoscaler), **zero zumbis** (o `restart=no` nos 24 containers antigos funcionou), inferências com engine OBB do cache.
- **Causa raiz de câmeras/controladores fora**: troca de placa-mãe (X870E, jul/2026) renomeou as NICs PCIe e o `01-netcfg.yaml` ficou com nomes antigos → `enp15s0` (vk01) e `enp17s0f*/enp18s0f*` (vk02) UP sem IPv4. Dongle USB-eth (`enx<MAC>`) sobreviveu — por isso o controlador do vk02 nunca caiu.
- **Fix aplicado pelo Pedro** (sed no 01-netcfg + netplan apply, mgmt intocado, backup `.bak-nicrename-20260713`): vk02 → 4 câmeras GigE adquirindo (DA3488401/02, DA3519240, DA3488405), ca-5..8 saíram do loop; vk01 → `enp15s0` com IP e carrier subiu, controlador 192.168.10.10 respondendo.
- PLC vk02 lendo dados reais (Velocidade_Tratada=0, linha parada). vk01 = câmeras USB (ca-1..4 estáveis, sem NIC — comportamento normal).
- Memória atualizada: `reference_03002_plant_network_map.md` (mapa completo de rede da planta + gotcha do rename).

## Decisões
- Netplan corrigido **in-place** no `01-netcfg.yaml` (preferência do Pedro), não em arquivo novo.
- Porta 502 fechada no controlador do vk01 não é falha (protocolo YHV não é Modbus TCP) — validação da camada Ethernet por ICMP/ARP era o objetivo.

## Pendências
- **Housekeeping de bancada nos hosts**: desabilitar `burnin-plant-ip.service` (runbook manda), sair do ZeroTier, deletar `99-local-test.yaml.bak*`, avaliar `burnin-reaper.timer`.
- **vk01 não loga no stdout dos containers** (docker logs vazio; vk02 normal) — investigar driver/config de logging.
- Validar pipeline com linha rodando (frames reais → OBB len=8 → visualizer) quando a produção retomar.
- vk03 (front+back novos) — deploy pendente, fora do nosso alcance direto.
- Fase 3-4 do throughput image-saver (PRs #75/#77 abertos; build 2026.08 + canary) — agora com a ressalva de que a bancada acabou: validar na própria planta em janela de manutenção ou linha parada.

## Links
- Memória: `reference_03002_plant_network_map.md`
- [[2026-07-12-vk-burnin-image-saver-throughput]] · PRs image-saver #75/#77 · issue #76

## Adendo (tarde) — burnin 30min, compose-padrão nos 2 hosts, rules engine e limpeza

- **6 containers do vk01 estavam "parked"** (`tail -f /dev/null` — entrypoint default das imagens!) desde a replicação da bancada: ca-1..4, controller, plc-monitor. Ninguém percebeu porque o burnin não usa entrada física. Corrigido no padrão definido pelo Pedro: **1 compose por container, moldado no yml antigo** (imagem/nome/binário novos + `NETWORK_ADAPTER=enp15s0` + `PART_UUID_FIELD_NAME`). vk02 padronizado igual (7 recriados; baks `-precompose-bak-20260713`). **Ambos os hosts 100% compose-managed.**
- **Validação física**: vk01 = controlador 192.168.10.10 com dados reais + PLC (4 hashes DB_* a 0s) + 4 câmeras USB adquirindo (ca-2 resolveu ao reconectar a DA3488407); vk02 = 4 câmeras GigE + controlador 192.168.50.12 + PLC ok.
- **Burnin 30min @150fps**: vk01 269.872 frames + 503.756 defeitos; vk02 269.996 + 234.000; **100% OBB len=8, zero erros** nos dois.
- **Achado → issue visionking-result#83**: `process_regras_complete_batch` (heartbeat 2s, sem lock) empilha execuções sob volume (5 simultâneas de 40s+), load 15,5, writers 150→39/s. Auto-recuperou ~15min após fim da carga; fix recomendado = `pg_try_advisory_xact_lock`.
- **Limpeza**: redis db0 flush + db2 streaming (`Camera_*`) nos 2 hosts; Pedro varreu ~760k dirs `BURNIN*` vazios (sobraram os 2k de hoje, reaper terminando); queries de purge do Postgres entregues ao Pedro (batched, ordem inspecao→defeitos→frames, discriminador `frame_path LIKE '%/BURNIN%'`).

## Adendo (noite) — rollback ca/is/ct, restauração do db2 e go pra produção

- **Rollback ordenado pelo Pedro** nos 2 PCs: volta para ca-sis-surface:c007, is-sis-surface:c007, ct-sis-surface:c004 (versões do mês passado), com 2 adaptações: is-sis publica nas filas renomeadas (inference/frame-queue) e ct usa NIC atual. Novos preservados como `*-rollback-hold-20260713` (restart=no). Bar-sim re-executado sobre o stack antigo: ct calculou trigger, ca capturaram, frames fluíram — **ciclo de aquisição OK nos antigos**.
- **Regressão da limpeza do burnin**: o flush do db2 (streaming) apagou os campos fixos (`frame_info` etc.) das chaves `Camera_*` — visualizer sem painéis. **Melhor referência = RDB de 19/06** dentro dos tars `_backup-jarvis-2026-06-19*` em `~/Downloads/img_saved` de cada PC (db/keydb-volume-snapshot/dump.rdb). Aplicado direto nos `cache` (stop→bak→swap→start): vk01 = Camera_1..4+inference (calibração jun), vk02 = Camera_5..8 (PONTO_2, seriais corretos).
- Gotcha: load de rdb no redistwo do 15.2 **falhou silencioso** (AOF prevalece sobre dump.rdb) e quase induziu conclusão errada; o container da planta chama-se `cache`, não `redis`.
- **frame_info do vk02 enriquecido a pedido**: title RAIO (cam 5) / FACE INFERIOR (6-8), `right/left/top/bottom=" "` p/ ocultar bolinhas; pipeline só escreve frame_info se ausente → HSET persiste. `Camera_6/7/8_inference` espelhadas das bases (chave só com frame_info derruba `/camera_images` inteiro — NoneType no serial).
- **Checklist pré-produção 2 hosts: VERDE** — restart policies ok (holds=no), autoscaler Up, filas 0 c/ consumers, db1 vivo, sim desfeita, 0 erros recentes, disco ok (869G/1,7T livres em img_saved). inference-B parado por scale-down do autoscaler (design). db0 vk01 tem cópias PLC de junho (inertes; trigger path comprovado via db1).

## Adendo (noite 2) — vk03: hotfix incremental c016_obb (front+back)

- **Pedido do Pedro**: as branches novas de front/back (auth real, users CRUD, JWTKEY) estão "erradas" p/ deploy agora — preparar **incremento mínimo sobre o c016 de abril só com o polígono OBB**.
- **Método**: labels `commit_sha` das imagens c016 deram as bases exatas (back `f88144b`, front `5b2fa9a`) → branches `hotfix/c016-obb-polygon` com cherry-picks limpos (back: `a32cbe5`, 1 arquivo `'position', d.position` nos 3 builders; front: 3 commits OBB, +161/−16). Commits do bot fantasma auditados e **excluídos**; grep de escopo (auth/login/mock/user) zero.
- **Build local**: gotchas — npm "Exit handler never called" = **DNS quebrado na bridge do docker** (EAI_AGAIN; systemd-resolved 127.0.0.53) → `docker build --network host` resolveu. Backend usa Dockerfile raiz (`--legacy-peer-deps`).
- **Transfer**: VPN SNX **caiu no meio do 1º scp** (falha silenciosa: mkdir 2>/dev/null + set -e não pegou) → 2ª leva com `md5sum -c` antes do load. Gotcha vk03: `http_proxy=127.0.0.1:3129` morto no host — curl precisa `no_proxy`.
- **Deploy**: padrão compose-por-container em `~/Downloads/c016_obb/` (ymls = cópia dos c016, só a tag); **rollback = `~/rollback-c016obb.sh`** (imagens+ymls c016 intactos). Par dev antigo intocado.
- **Smoke com produção viva** (linha voltou 13/07 à noite, corrida F66401800213): front 200; `GET /frames/1/defeitos/barra/F66401800213` → defects com `position` **len=8** real `[938,605,1003,606,1002,663,937,662]` + x1..y2 compat. Falta o Pedro conferir o polígono no browser.

## Pendências (atualizadas)
- Housekeeping bancada nos hosts: `burnin-plant-ip.service`, ZeroTier, netplan `.bak`s, reaper timer (desabilitar após purge final?).
- Pedro roda as queries de purge do DB (vk02 pronto; vk01 pronto desde 16:04).
- issue #83 (rules engine) — outra sessão.
- Repetir `-empty -delete` nos 2k dirs de hoje quando o reaper terminar.
- Motivo do rollback dos novos ca/is/ct a esclarecer; re-upgrade planejado depois.
- Reverter dump.rdb do redistwo no 15.2 (`dump.rdb.bak-20260713`) se quiser preservar o clone histórico.
