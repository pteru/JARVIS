---
type: Session Log
title: 2026-07-12 — iris-plc — v5.2→v5.5 sobre o refactor ServoAxis do Willer + EDS 03007 + emulador 43/43
description: Dia inteiro do 03007 — Willer refatorou o Servo p/ UDT_ServoAxis (2 entregas, a segunda export Studio genuíno após análise forense da primeira); rebaseamos jog/homing 4×(v5.2→v5.5), fechamos mm em REAL, unificamos o jog, adicionamos init formal + ciclo c/ jobdata + gestão de falhas com first-out; EDS variant iris03007 gerado (D1–D7 fechadas) e tudo validado no emulador sdk-line-twin (43/43).
tags: [iris-plc, iris-scds, 03007, plc, rockwell, l5x, eds, visionking]
timestamp: 2026-07-12
session: iris-plc
project: "03007"
product: visionking
language: pt-BR
---

# 2026-07-12 — iris-plc (v5.2→v5.5 + EDS + emulador)

## Feito

- **Snapshots Willer 12/07** (`plc/`): 13:32 = refactor `UDT_ServoAxis`
  (In/Out PDO, AxisIn/AxisOut, Cmd/Cfg/Ramp, `ZeroOffset`, ponte Cmd←AxisOut,
  R021_DynamicTracking, R051 jog+Zero_Button); **análise forense** provou que
  o arquivo era o export r2 editado como texto (ExportDate idêntico, dialeto
  misto, `BOOL[5]`, `ZeroOffset` fora de `<Members>`, sem L5K). Pedro cobrou o
  export real → **r2 15:38 = Studio genuíno** (335 controller tags no CSV;
  R003 corrigido por ele, R051_Inverters renomeada, `ZeroOffset` REAL, Cfg no
  R001, task 8 ms). Programa existe e compila no ACD; 13:32 era artefato.
- **v5.2→v5.3**: jog/homing/mm re-expressos DENTRO do `UDT_ServoAxis`
  (RefValid bit2 do host, `ZeroOffset` = variável de home dele, COPs por
  membro, sync da ponte pós-jog/homing). v5.2 descartado ao chegar o r2.
- **v5.4** (decisões Pedro): interface mm em **REAL** (`ActualPos`/`TargetPos`/
  `TargetPositionCmd`) + janela R020 ±0,5 mm; typos (`Initialize`,
  `TargetPos`×56, `TouchProbeFunc`); **jog unificado** (Seção 3 executor
  único; R030 com 2 fontes — WS deadman + botões locais hold-to-run, botão
  vence; R051 só-tara); entrega vira **2 programas completos** (Import
  Program via Task); auditoria de tags (82 deletar + 14 confirmar Fase A +
  145 manter). 24/24 no emulador.
- **v5.5**: `R011_Initialize` formal (T_clear 5 s → homing global via R031 →
  eixos abertos → zonas → WS/GM; `St_MaquinaOK` fecha INIC→AUTO); R020 com
  entrada formal + abort + **jobdata real** (UDTs v2 `Station[11]`,
  `R006_MapGM` watchdog, `R090_GMSim`); `R004_AlarmManager` (first-out
  `IRIS_FaultCode`, word/count/severity, reset por causa); `R052_Publish`
  (heartbeat IRIS→GM, Ready/Fault/**FaultCode**/**ConveyorOK** §0.1 — GM_Out
  +2 campos, coordenar com GM). **43/43** no emulador (24 regressão + 19
  init/ciclo/falhas). Erro de import do Willer resolvido: era Import Routine
  × Import **Program** (README explicitado).
- **EDS iris03007**: Anexo D v0.2 (D1 RPI 5 ms · D2 unicast · D3 hot-bridge ·
  D4 Param por bit · D5 tick hot-bridge · D6 inerte + issue · D7 arquivo
  próprio identity 1.1); `STROKMATIC-COMM-V1_iris03007.eds` (78 Params,
  1024+1024 bits validados) + ícone 32×32 em strokmatic-eip e
  `reference/eds/`; **issue strokmatic-eip#12** (config assembly inerte).

## Achados do emulador (valor real)

1. **Ponte engole retarget**: alvo escrito durante movimento era descartado
   (edge perdido no gate `Ramp.State=0`) — pego no ciclo automático (STEP.4);
   fix = ponte só consome com eixo pronto (retarget enfileirado).
2. R020 re-entrava no ciclo com falha ativa (faltava `XIO(IRIS_Fault)`).
3. **DriveModel do sdk-line-twin assume fio PADDED** — contradiz o contrato
   C3 §3.1 (tight; janela 21 B fecha exata; Willer confirmou ActualPosition
   @7). Smokes rodaram com patch tight → **escalar à sessão line-twin**.
4. COP de bloco lê posição ÷256 (padding @8 × fio @7) e "parece funcionar" —
   teste de bancada de 1 min no README (fator 256).

## Decisões (Pedro)

- mm em REAL na interface Main↔Servo; janela ±0,5 mm; typos corrigidos já.
- Jog: executor único (Seção 3), 2 fontes com botão local vencendo a WS.
- Entrega = 2 L5X completos + CSV (pré-condição: não mexer no ACD entre
  export e import; senão re-export + rebase por script).
- Deleção de tags: 82 + 14 (após confirmar configs da Fase A); executar no
  tag editor (import não deleta).
- ConveyorOK inclui "ciclo saudável em curso" (eixos acompanham o carro) —
  TODO validar semântica final com GM.
- EDS: D1–D7 conforme acima; FaultCode entra no R052.

## Pendências

- Willer: importar v5.5 (Import Program via Task) + deletar as 82 tags +
  bancada (checklist 8 passos; teste do fator 256 decide 7×8 definitivo).
- GM: coordenar GM_Com_Out +ConveyorOK+FaultCode (layout produced/consumed).
- Escalar ao line-twin: divergência PADDED×tight do DriveModel + suporte a
  `ABS()` no parser ST (R021 precisou de stub nos smokes).
- Fase A (plc-monitor): confirmar que nenhum config referencia as 14 tags
  "superseded" antes de deletar.
- Anexo D → integrar ao contrato §2.4 + render; hot-bridge (implementação).
- Especialista OKF `iris-plc` fora do roster — propor criação.

## Links

- Commits: pmo `e7a10b4` (+fixup README) · strokmatic-eip `d65b743`
- Pacotes: `handoffs/2026.07.12_update-jog-homing-v5.4|v5.5/` (zips no scratchpad)
- Snapshots: `plc/2026.07.12_programa-plc-rockwell[-r2]/` (READMEs = diff+forense)
- EDS: `reference/eds/STROKMATIC-COMM-V1_iris03007.eds` · issue strokmatic-eip#12
- Journals irmãos: [[2026-07-11-iris-plc]] · [[2026-07-12-line-twin]]
