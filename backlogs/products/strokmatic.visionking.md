# VisionKing Backlog

> Detailed implementation specs for each task: [`doc/backlog-specs.md`](../doc/backlog-specs.md)

## High Priority
- [ ] [MEDIUM] SEALER-09: Create 3D sealer pipeline integration tests — E2E tests following established VK test infrastructure (pytest markers: contract, benchmark, service, pipeline). Docker Compose profile sealer-broker: RabbitMQ:5676, Redis:6383, PG:5436 (no collision with VK:5673/6380/5433). Contract tests: Redis HASH 3D schema, all 5 RabbitMQ message schemas (per SEALER-11), PG sealer tables. Pipeline tests: synthetic point cloud → sealer-raw-queue → processing → measurement → PG verification. Benchmarks: processing latency < 500ms for 1M points. Synthetic fixtures: generate_test_pointclouds.py creates known-geometry PLY (flat plane, cylinder, step). Acceptance: (1) contract tests for all schemas, (2) pipeline E2E with synthetic data, (3) benchmarks with targets, (4) dedicated test ports, (5) fixtures generated programmatically. New: tests/e2e/sealer/, tests/e2e/contracts/test_sealer_*.py, tests/e2e/pipelines/test_sealer_e2e.py. Depends on: SEALER-11, SEALER-08 (output format).
- [ ] [COMPLEX] SEALER-08: Create hikrobot-acquisition service — New C++ service for Hikrobot MV-DLS1400M-15 galvanometric laser 3D camera using proprietary MV3D SDK (NOT GenICam). Outputs to Redis DB0 HASH: point_cloud_data (binary PLY), depth_map (binary float32), optional rgb_image (JPEG), plus standard fields (frame_uuid, camera_serial_number, part_uuid, frame_cell, timestamps). Publishes metadata to sealer-raw-queue (RabbitMQ). Config via HKRBT_* env vars (CAMERA_IP, SCAN_SPEED, LASER_POWER, TRIGGER_MODE, EXPOSURE_TIME, SCAN_LINE_COUNT). Docker image on Ubuntu 22.04 with MV3D SDK libs. Structure mirrors camera-acquisition (C++20, Meson, redis++, jsoncpp). Acceptance: (1) connects to camera via GigE, (2) acquires 3D point cloud, (3) writes to Redis HASH with correct fields, (4) publishes to RabbitMQ, (5) configurable scan parameters. New: services/hikrobot-acquisition/. Depends on: none. Feeds: SEALER-01, SEALER-02.
- [ ] [COMPLEX] SEALER-07: Create sealer-visualization Angular module — New lazy-loaded Angular module for 3D sealer inspection. Three.js point cloud viewer with pan/orbit/zoom and color-coded defect markers (green=pass, yellow=warning, red=fail). Bead cross-section chart (echarts, already a dependency) shows measured profile vs tolerance bands at user-selected points. Dashboard: pass/fail summary per vehicle, segment-level detail table, defect rate trend charts. Dark theme consistent with existing SCSS vars. Route added to portal.routes.ts via loadChildren. Dependencies: three, @types/three (echarts already present). Acceptance: (1) 3D viewer renders point cloud < 1M points, (2) interactive cross-section selection, (3) pass/fail dashboard with PrimeNG table, (4) trend charts, (5) lazy-loaded route. New: frontend/src/app/modules/portal/sealer-visualization/. Depends on: SEALER-06. Can start with mock data.
- [ ] [MEDIUM] SEALER-06: Create sealer-inspection NestJS module — New backend module with Sequelize entities SealerMeasurement and SealerInspectionSummary. REST endpoints: GET /api/v1/sealer/inspections (paginated summaries with date_range, pass_fail, station_id filters), GET /api/v1/sealer/inspections/:id (detail with all segments), GET /api/v1/sealer/measurements/:partUuid (part measurements), GET /api/v1/sealer/trends (aggregated pass/fail rates, avg width/height over configurable time window). Follow inspected-products module pattern: entity + module + controller + service interface + service + provider + repository + DTO. Register in AppModule imports. Swagger ApiTags. Acceptance: (1) 4 endpoints functional, (2) pagination + filters, (3) trend aggregation SQL, (4) Swagger documented, (5) no impact on existing modules. New: backend/src/modules/sealer-inspection/. Depends on: SEALER-05.
- [ ] [MEDIUM] SEALER-05: Adapt database-writer for sealer measurements — Add DB_WRITER_MODE=sealer alongside existing default and sparktest modes. New PG tables: sealer_measurements (frame_uuid FK, segment_id, width_mm DECIMAL(6,2), height_mm, continuity_pct, volume_mm3, position_offset_mm, pass_fail BOOLEAN, tolerance_mm, measurement_time_ms, inserted_at) and sealer_inspection_summary (part_uuid UNIQUE, total_segments, passed_segments, overall_pass_fail, model_type VARCHAR, station_id, inspection_timestamp). New stored procedure insert_sealer_measurements(jsonb) with single-transaction insert + summary upsert. Consumes sealer-measurement-queue. Follow sparktest module pattern: src/sealer/{models.py, db_writer.py, consumer.py}. Acceptance: (1) tables created via migration 003_sealer_tables.sql, (2) stored procedure handles batch segment insert, (3) Pydantic validation on inbound messages, (4) invalid messages NACK'd without requeue. Depends on: SEALER-03. Feeds: SEALER-06.
- [ ] [COMPLEX] SEALER-04: Create 3D inference training pipeline — Hybrid defect detection: geometric rules (from SEALER-03 bead measurements) + YOLOv11 ML on 2D depth map projections. Training pipeline in ds/sealer-training/: extract depth map tiles from .ply (5-8 tiles/frame, 10% overlap, ROI mask), annotate (CVAT/Label Studio → YOLO format), train YOLOv11, export ONNX + OpenVINO IR for CPU inference on AAEON Boxer-6641 (Xeon E-2124G, no GPU). Defect classes: gap, thin, thick, offset, splash, nominal. Inference target: < 200ms/tile. Phase 1 deploys geometric rules only; Phase 2 adds ML model when training data sufficient. Acceptance: (1) depth map projection pipeline, (2) tiling with configurable overlap+ROI, (3) ONNX export + OpenVINO conversion scripts, (4) training config yaml, (5) integration path with existing inference service (openvino_engine.py). New: ds/sealer-training/. Depends on: SEALER-01, SEALER-03.
- [ ] [COMPLEX] SEALER-03: Create sealer-bead-measurement service — New Python microservice (async RabbitMQ consumer pattern from database-writer) that consumes processed point clouds from pc-processed-queue, extracts bead geometry via cross-section analysis: centerline detection (PCA), perpendicular slicing at 5mm intervals, Gaussian profile fitting per section. Per-segment output: width_mm, height_mm, continuity_pct, volume_mm3, position_offset_mm, pass_fail. Pass/fail: segment fails if width < 3mm OR height < 1.5mm OR continuity < 80%. Overall part fails if any segment fails. Publishes to sealer-measurement-queue as JSON with segments array + overall_pass_fail. Acceptance: (1) consumes pc-processed-queue, (2) cross-section extraction at configurable intervals, (3) Gaussian/parabolic profile fitting, (4) per-segment pass/fail with 3mm tolerance, (5) published message includes all measurement fields. New service: services/sealer-bead-measurement/. Depends on: SEALER-01. Feeds: SEALER-05.
- [ ] [MEDIUM] SEALER-02: Adapt image-saver for 3D point cloud storage — Extend existing image-saver to detect data type from Redis HASH (image_streaming vs point_cloud_data field) and save as .ply/.pcd for point clouds or .bin for images. Path: /img_saved/{part_uuid}/{camera_serial_number}/{hash}.{ext}. RabbitMQ message includes data_type field (image|pointcloud). New env vars: SUPPORTED_DATA_TYPES, POINTCLOUD_FORMAT. Add plyfile dependency (lightweight, no Open3D needed). Acceptance: (1) auto-detects 2D vs 3D data, (2) saves .ply for point clouds, (3) backward-compatible with all 2D flows, (4) existing tests pass, (5) new unit tests for pointcloud_saver. Modified: services/image-saver/src/hash_processor.py, new: src/image_saver/pointcloud_saver.py. Depends on: none. Parallel with SEALER-01.
- [ ] [COMPLEX] SEALER-01: Create point-cloud-processor service — New Python microservice that consumes raw 3D point cloud data from Redis DB0 HASHes (field point_cloud_data, binary PLY), applies statistical outlier removal (Open3D), RANSAC ground plane fitting, ROI segmentation, and depth map generation. Publishes processed cloud to pc-processed-queue (RabbitMQ). Stores processed .ply to /img_saved/{part_uuid}/{camera_serial_number}/{hash}.ply and depth maps as .npy. Follows image-saver architectural pattern (Redis polling, thread pool, RabbitMQ publishing). Config via PC_PROC_* env vars. Dependencies: Open3D, numpy, scipy. Acceptance: (1) consumes Redis HASHes with point_cloud_data field, (2) 4-stage pipeline completes (deserialize→outlier removal→plane fit→ROI→depth map), (3) publishes JSON to pc-processed-queue, (4) .ply files persisted, (5) healthcheck at /healthz. New service: services/point-cloud-processor/. Depends on: none. Feeds: SEALER-03, SEALER-04.
- [ ] [MEDIUM] SPARK-07: Adapt database-writer for spark test results — New RabbitMQ queue (spark-result-queue) and PostgreSQL insert function for spark test results. Reuse existing database-writer with new config or deploy separate container instance
- [ ] [MEDIUM] SPARK-06: Add SparkTest topology profile — New topology template sparktest-single-node.yaml. Extend topology-configurator for wifi-rtsp camera type and SparkTest profile. Extend deployment-runner for SparkTest docker-compose generation
- [ ] [COMPLEX] SPARK-05: Create frontend-sparktest Angular tablet PWA — Tablet-optimized dark-theme touch UI. Screens: material input (keyboard + OCR), capture control (Start/Analyze), result display (alloy + APROVADO/REPROVADO), test history. Offline-capable
- [ ] [COMPLEX] SPARK-04: Create backend-sparktest NestJS API — Gateway for tablet frontend. Proxy to spark-test-controller. CRUD for materials catalog, test history, operators. WebSocket for real-time updates. New PostgreSQL schema (materials, spark_tests, spark_test_frames)
- [ ] [COMPLEX] SPARK-03: Adapt inference service for spark classification — Configure existing ONNX/TensorRT inference for spark classification model. Support pluggable strategies: per-frame+aggregation, video clip, key-frame selection. Placeholder model initially
- [ ] [COMPLEX] SPARK-02: Create spark-test-controller service — Python/FastAPI orchestrator for test lifecycle. REST API for start/analyze/result. State machine IDLE→CAPTURING→ANALYZING→COMPLETE/FAILED. Generates sample UUIDs, coordinates camera, publishes to RabbitMQ, compares identified vs expected material (APROVADO/REPROVADO)
- [ ] [COMPLEX] SPARK-01: Create wifi-camera-acquisition service — Python service connecting to N RTSP streams from WiFi cameras (DJI Action 4). Extracts frames at configurable FPS, writes to Redis DB0 as HASHes (same format as existing camera-acquisition). Start/stop via Redis pub/sub. Design: docs/plans/2026-03-04-sparktest-deployment-profile-design.md
- [ ] [complex] SEC-01: Rotate and remove hardcoded credentials — `.env.example` has `skm@@2022`, `SisSurface@@2022`. GCP JSON keys in repo root + inference/secrets.
- [x] - [ ] [complex] TEST-01: Add automated test suites for all Python services — Only database-writer has pytest. 6+ services at 0% coverage. Target 80% on business logic. (Completed: 2026-02-26)
- [ ] [complex] TEST-02: Add test infrastructure for C++ services — camera-acquisition, controller, plc-monitor have no gtest, no CI test step.
- [ ] [medium] GIT-01: Resolve uncommitted submodule changes — 5 submodules with uncommitted/unpushed work: controller, frontend, length-measure, plc-monitor, result.
- [ ] [complex] CICD-01: Add Cloud Build configs to missing services — Missing: pixel-to-object, length-measure, backend, frontend, inference (5 of 17).
- [ ] [medium] CICD-02: Implement Docker build caching and multi-stage optimization — No --cache-from, no BuildKit. Target 50%+ build time reduction.
- [ ] [medium] DOC-01: Add README.md to all services missing documentation — ~8 services undocumented: image-saver, defect-aggregator, controller, length-measure, etc.
- [ ] [medium] DOC-02: Create deployment runbook — No operational docs. Cover topology workflow, troubleshooting, backup/recovery.
- [ ] [complex] HEALTH-01: Add Docker healthchecks to all services — Only backend-ds has a healthcheck. 16 services unmonitored.

## Medium Priority
- [ ] [COMPLEX] SEALER-12: Create sealer deployment topology — New topology template topologies/sealer-single-node.yaml (schema v1.0) for AAEON Boxer-6641. Service catalog additions: hikrobot-acquisition (category=acquisition, profiles=[sealer], env_prefix=HKRBT_), point-cloud-processing (category=processing, profiles=[sealer], env_prefix=PCP_), sealer-bead-measurement (category=processing, profiles=[sealer], env_prefix=SBM_). OpenVINO EP for CPU inference (no GPU on Boxer). Network: dedicated GigE NIC for camera (192.168.10.x, jumbo frames), Profinet NIC for PLC (192.168.20.x). Commissioning script: scripts/commission-sealer.sh checks service health, camera connectivity, PLC reachability, test scan, measurement validation vs calibration reference. Acceptance: (1) topology generates valid docker-compose, (2) service catalog extended, (3) commissioning script validates full pipeline, (4) network config documented, (5) deployment guide. New: topologies/sealer-single-node.yaml, scripts/commission-sealer.sh, doc/sealer-deployment-guide.md. Depends on: SEALER-08, SEALER-11, SEALER-10.
- [ ] [SIMPLE] SEALER-11: Define sealer RabbitMQ queue topology — Topic exchange sealer-exchange with 5 queues: sealer-raw-queue (sealer.raw, from hikrobot-acquisition), sealer-processed-queue (sealer.processed, from point-cloud-processor), sealer-measurement-queue (sealer.measurement, from bead-measurement), sealer-inference-queue (sealer.inference, for ML defects), sealer-result-queue (sealer.result, merged for database-writer). Dead-letter exchange sealer-dlx with sealer-dead-letter-queue (60s TTL retry). JSON message schemas documented per queue with field names, types, examples. Declarations in rabbitmq-definitions.json for test env. Acceptance: (1) exchange + 5 queues + DLX defined, (2) routing keys documented, (3) message schemas with examples, (4) definitions.json for testing, (5) doc/sealer-queue-topology.md. Depends on: none.
- [ ] [MEDIUM] SEALER-10: Create 3D camera calibration tool — Python CLI tool for calibrating the Hikrobot 3D camera setup. Calibration target: machined aluminum reference block with 3 beads of known width (3/5/8mm) and height (2/4/6mm). CLI: python -m sealer_calibration calibrate --camera-ip <IP> --output <path>. Sequence: acquire reference scan → detect features → compute 4x4 rigid body transform (ICP/SVD) → validate measurements → save JSON. Calibration JSON: {camera_serial, calibration_date, transform_matrix_4x4, scale_factors_xyz, measurement_offsets, validation_results, status}. Validation: width/height error < 0.5mm, position error < 1.0mm, RMS < 0.2mm. Loaded at startup via HKRBT_CALIBRATION_FILE env var. Acceptance: (1) CLI performs full calibration, (2) JSON schema persisted, (3) validation tolerances enforced, (4) calibration fail alerts, (5) operator documentation. New: services/sealer-calibration/. Depends on: SEALER-08.
- [ ] [medium] CLEAN-01: Remove large artifacts and organize untracked files — 300MB+ zip in root, 2GB+ legacy/, 5,889 __pycache__ dirs.
- [ ] [medium] CLEAN-02: Implement model artifact management (DVC/GCS) — inference repo is 7.5GB, models stored directly in git.
- [ ] [medium] INFRA-01: Implement centralized logging — JSON file logging only, no aggregation. Options: gcplogs or Loki+Grafana.
- [ ] [medium] INFRA-02: Standardize network configuration — Mixed host/bridge networking. Audit which services truly need host mode.
- [ ] [medium] OPT-01: Add RabbitMQ dead-letter queues and message batching — No DLX configured. Failed messages may be silently dropped.
- [ ] [medium] OPT-02: Add resource monitoring and auto-scaling thresholds — No container metrics, no GPU monitoring on inference.
- [ ] [medium] QUAL-01: Standardize error handling and logging — Inconsistent patterns. Need shared decorator + loguru standardization.
- [ ] [simple] QUAL-02: Add API documentation (OpenAPI/Swagger) — NestJS backends have no Swagger setup.

## Low Priority
- [ ] [medium] TOOL-01: Integrate data science pipeline into CI/CD — ds/ folder isolated. Need cloudbuild + model export automation.
- [ ] [medium] TOOL-02: Complete and document cloud pipeline toolkit — cloud-pipeline-draft/ incomplete. Evaluate or archive.
- [ ] [simple] DOC-03: Create developer onboarding guide — No local dev setup docs, no submodule init guide.
